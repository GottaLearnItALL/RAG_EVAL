"Act as an expert web designer and frontend developer. Write the complete HTML, CSS (using Tailwind CSS via CDN), and basic JavaScript code for a modern, responsive chatbot webpage.Design Requirements:Layout: A clean, centered chat interface with a fixed header, a main scrollable message area, and a sticky input footer at the bottom.Color Scheme: A modern dark or light mode theme with a distinct primary accent color for user messages and bot replies.Features:A scrollable chat history area with distinct styling for user bubbles and AI bubbles.A text input box with a functional 'Send' button (and support for pressing the Enter key).Auto-scrolling to the newest message.A simple JavaScript mock function that simulates a typing indicator and a basic response from the bot.Output: Provide a single, clean, production-ready HTML file containing all styles and scripts so I can copy and open it immediately."

Markup — a textarea, a button, an empty div for results.
JS handler — on click, fetch("/ask", {method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({question})}), await the JSON, write it into the results div.